[module/pkg-updates]
type = custom/script
exec = $HOME/.config/polybar/scripts/updates-pacman-aurhelper.sh
interval = 600

format-prefix = "  "
format-prefix-foreground = ${colors.secondary}

label-foreground = ${colors.primary}

click-left = $TERM -e $SHELL -c 'paru' &